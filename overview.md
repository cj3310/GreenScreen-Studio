# 绿幕抠图工作台 — 交付概览

## 完成内容

构建了一个完整的桌面绿幕抠图 GUI 工具（Python + PySide6 + OpenCV），覆盖需求清单全部七大模块。

## 关键文件

| 文件 | 职责 |
|------|------|
| `main.py` | 程序入口，启动 QApplication 与主窗口 |
| `run.bat` | Windows 一键启动（已绑定独立 venv） |
| `app/keying_engine.py` | 抠图核心算法：HSV 抠除、形态学收缩/扩张、高斯羽化、边缘平滑、裁切、RGBA 合成、透明 PNG 导出、吸管取色、预设持久化 |
| `app/canvas_widgets.py` | 棋盘格预览画布、交互式裁切选框（拖动/控制点调整/重置）、吸管取色坐标映射 |
| `app/control_panels.py` | HSV 六滑块、羽化/收缩扩张/平滑滑块、蒙版预览开关、时间轴、导出配置面板 |
| `app/main_window.py` | 主窗口：拖拽导入、双画布同步、视频管理、播放控制、后台导出线程、错误弹窗、键盘快捷键 |

## 验证结果

- ✅ 所有模块导入无误
- ✅ 主窗口在 offscreen 模式下成功构建
- ✅ 抠图引擎：背景 alpha=0（绿幕干净抠除）、主体 alpha=255（保留）
- ✅ 裁切 + 抠图：输出尺寸正确
- ✅ 吸管取色：生成合理 HSV 范围
- ✅ 视频加载→读帧→抠图→显示 全流程通过
- ✅ 跳帧、参数实时刷新、逐帧步进均正常
- ✅ 批量导出：60 帧间隔 10 → 6 张 `frame_000001.png`…RGBA 四通道、背景透明
- ✅ 进度信号、完成/失败回调正常

## 运行方式

双击 `run.bat`，或拖入 `sample_green_screen.mp4` 立即体验。

## 备注

- 项目目录下的 `test_out.png` 与 `export_test_out/` 为验证产物，沙箱环境无法删除，可手动清理，不影响应用运行（应用只写入不删除）。
